server {
    listen 80;
    server_name ucs.bittonik.com;

    # Redirection HTTP vers HTTPS (à activer après avoir installé le certificat SSL)
    # return 301 https://$host$request_uri;

    # Si vous n'avez pas encore de SSL, décommentez le bloc location ci-dessous 
    # et commentez le bloc return 301 ci-dessus pour tester en HTTP d'abord.

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        
        # Transmission de l'IP réelle de l'utilisateur
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Bloc HTTPS (Géré automatiquement par Certbot)
# server {
#     listen 443 ssl http2;
#     server_name ucs.bittonik.com;
#     ...
# }
